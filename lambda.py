
#lambda functions in python

#syntax is "lambda arguments : expression

area_square = lambda x : x * x
print(area_square(5))

#the above is the program to find the area of square using lambda


def area(x):
    x = x * x
    print(x)
area(5)

#above is the same operation to find area of square but with def function
